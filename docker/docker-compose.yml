version: "3.8"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - BEHAVIOR_MODEL_PATH=/app/models/driver_behavior_model.joblib
    volumes:
      - ./models:/app/models